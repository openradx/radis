def sort_out_documents(query: str, question: str, documents: list) -> list:
    # TODO: documentwise evaluation via llm -> discard all documents, which are not relevant to the
    # question
    return documents
